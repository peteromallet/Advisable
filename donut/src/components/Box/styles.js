import styled from "styled-components";
import { space, layout, color, flex, position } from "styled-system";

const Box = styled.div`
  ${flex}
  ${color}
  ${space}
  ${layout}
  ${position}
`;

export default Box;
