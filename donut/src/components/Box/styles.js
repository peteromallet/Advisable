import styled from "styled-components";
import { space, border, layout, color, flex, position } from "styled-system";

const Box = styled.div`
  ${flex}
  ${color}
  ${space}
  ${layout}
  ${border}
  ${position}
`;

export default Box;
