<div class="py-5 first:pt-0">
  <p class="font-semibold text-gray-800 mb-1"><%= insight.title %></p>
  <p class="leading-relaxed text-sm text-gray-700 mb-1"><%= insight.description %></p>
  <%= link_to "Delete", remove_insight_admin_article_path(insight.article, insight_id: insight.id), class: "text-neutral-500 text-sm hover:text-neutral-900", data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
</div>
