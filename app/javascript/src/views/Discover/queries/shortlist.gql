#import "./articleFields.gql"

query Shortlist($id: ID!) {
  currentCompany {
    id
    kind
    name
    industry {
      id
      name
    }
  }
  interest(id: $id) {
    id
    name
    results(first: 100) {
      nodes {
        ...ArticleFields
      }
    }
  }
}
