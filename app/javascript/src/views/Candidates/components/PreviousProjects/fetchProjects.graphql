query PreviousProjects($applicationId: ID!) {
  application(id: $applicationId) {
    airtableId
    referencesRequested
    specialist {
      id
      name
    }
    previousProjects {
      id
      type
      title
      description
      companyName
      reviews {
        id
        ratings {
        	overall
        }
      }
    }
  }
}