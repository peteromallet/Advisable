mutation updateApplicationStatus(
  $id: ID!
  $status: String!
  $rejectionReason: ID
) {
  updateApplicationStatus(
    input: { id: $id, status: $status, rejectionReason: $rejectionReason }
  ) {
    application {
      id
      status  
    }
  }
}
