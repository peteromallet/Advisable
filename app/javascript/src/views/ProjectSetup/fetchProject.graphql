query project($id: ID!) {
  project(id: $id) {
    id
    airtableId
    goals
    status
    questions
    description
    depositOwed
    acceptedTerms
    companyDescription
    specialistDescription
    requiredCharacteristics
    optionalCharacteristics

    skills {
      id
      name
    }
  }
}