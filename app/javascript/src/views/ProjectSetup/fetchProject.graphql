query project($id: ID!) {
  project(id: $id) {
    id
    airtableId
    goals
    status
    questions
    description
    depositOwed
    acceptedTerms
    primarySkill
    companyDescription
    specialistDescription
    requiredCharacteristics
    optionalCharacteristics
  }
}