query Application($id: ID!) {
  application(id: $id) {
    id
    airtableId
    status
    rate
    proposal {
      id
      airtableId
    }
    project {
      id
      currency
      primarySkill
      user {
        companyName
      }
    }
  }
}
