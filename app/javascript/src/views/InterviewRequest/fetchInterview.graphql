query interview($id: ID!) {
  interview(id: $id) {
    id
    status
    startsAt
    timeZone
    user {
      id
      availability
    }
    application {
      id
      specialist {
        id
        phoneNumber
      }
      project {
        id
        client {
          id
          name
        }
      }
    }
  }
}
