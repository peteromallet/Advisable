query Application($id: ID!) {
  application(id: $id) {
    id
    airtableId
    status
    rate
    proposalComment
    tasks {
      id
      airtableId
      name
      stage
      dueDate
      description
      estimate
    }
    project {
      id
      currency
      primarySkill
      user {
        companyName
      }
    }
  }
}
