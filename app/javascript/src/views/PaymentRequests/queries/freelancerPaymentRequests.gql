query FreelancerPaymentRequests($cursor: String) {
  paymentRequests(after: $cursor, first: 20) {
    pageInfo {
      endCursor
      hasNextPage
    }
    edges {
      node {
        id
        status
        amount
        createdAt
        company {
          id
          name
        }
        lineItems {
          amount
          description
        }
      }
    }
  }
}
