query recommendationsInbox($id: ID!) {
  caseStudySearch(id: $id) {
    id
    name
    results {
      nodes {
        id
        title
        subtitle
        isArchived(id: $id)
        specialist {
          id
          avatar
        }
      }
    }
  }
}
