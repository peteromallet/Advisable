query Application($id: ID!) {
  application(id: $id) {
    id
    status
    referralUrl
    airtableId
    project {
      id
      airtableId
      name
      primarySkill
      companyDescription
      goals
      requiredCharacteristics
      optionalCharacteristics
      estimatedBudget
      remote
      user {
        companyName
        country {
          name
        }
      }
    }
  }
}
