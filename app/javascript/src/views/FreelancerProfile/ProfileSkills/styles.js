import styled from "styled-components";
import { theme } from "@advisable/donut";

export const StyledProfileSkill = styled.div`
  font-size: 13px;
  padding: 7px 12px;
  margin-right: 4px;
  margin-bottom: 4px;
  border-radius: 20px;
  display: inline-block;
  color: ${theme.colors.blue[9]};
  background: ${theme.colors.blue[1]};
`;
