mutation declineBooking($id: ID!, $reason: ID!, $declineComment: String) {
  declineBooking(id: $id, reason: $reason, declineComment: $declineComment) {
    booking {
      id
      status
    }
  }
}
