mutation declineBooking($id: ID!, $reason: String!, $declineComment: String) {
  declineBooking(id: $id, reason: $reason, declineComment: $declineComment) {
    booking {
      id
      status
    }
  }
}
