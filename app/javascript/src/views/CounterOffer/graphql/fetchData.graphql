query project($bookingID: ID!) {
  booking(id: $bookingID) {
    id
    type
    rate
    rateType
    rateLimit
    duration
    startDate
    endDate
    deliverables
    application {
      id
      project {
        id
        name
        currency
      }
      specialist {
        id
        name
      }
    }
  }
}
