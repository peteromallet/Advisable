query booking($id: ID!) {
  booking(id: $id) {
    id
    type
    rate
    rateType
    duration
    status
    startDate
    endDate
    deliverables
    application {
      id
      project {
        id
        name
        currency
        client {
          name
        }
      }
    }
  }
}
