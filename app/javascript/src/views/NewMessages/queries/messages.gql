query ConversationMessages($id: ID!, $cursor: String) {
  conversation(id: $id) {
    id
    unreadCount
    messages(last: 50, before: $cursor) {
      pageInfo {
        hasPreviousPage
        startCursor
      }
      edges {
        node {
          id
          content
          createdAt
          status @client
          attachments {
            id
            url
            filename
          }
          author {
            id
            name
            avatar
          }
        }
      }
    }
  }
}
