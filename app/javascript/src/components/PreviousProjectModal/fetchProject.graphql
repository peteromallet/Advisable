query PreviousProject(
  $id: ID!
  $type: PreviousProjectTypeAttribute!
  $applicationId: ID!
) {
  previousProject(id: $id, type: $type, applicationId: $applicationId) {
    id
    type
    title
    description
    companyName
    clientDescription
    requirements
    results
    reviews {
      id
      name
      role
      comment
      ratings {
        overall
        skills
        qualityOfWork
        adherenceToSchedule
        availability
        communication
      }
    }
  }
}
