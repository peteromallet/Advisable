mutation RequestIntroduction($input: RequestIntroductionInput!) {
  requestIntroduction(input: $input) {
    interview {
      id
      status
      application {
        id
        status
      }
    }
    errors
  }
}
