mutation createTask($input: CreateTaskInput!) {
  createTask(input: $input) {
    task {
      id
      stage
      name
      estimate
      dueDate
      description
      airtableId
      createdAt
      application {
        id
      }
    }
    errors {
      code
    }
  }
}