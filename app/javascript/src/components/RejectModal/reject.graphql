mutation rejec($id: ID!, $rejectionReason: ID) {
  updateApplicationStatus(
    input: {
      id: $id
      status: "Application Rejected"
      rejectionReason: $rejectionReason
    }
  ) {
    application {
      id
      status
    }
  }
}
