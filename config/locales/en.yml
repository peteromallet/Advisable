en:
  errors:
    format: "%{message}"
  activerecord:
    errors:
      models:
        user:
          attributes:
            email:
              taken: "users.email_taken"
              invalid: "users.invalid_email"
              minimum: "users.password_too_short"
