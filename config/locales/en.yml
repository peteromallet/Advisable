en:
  activerecord:
    errors:
      models:
        user:
          attributes:
            email:
              taken: "is already being used by another account"
              invalid: "is not a valid email"
            password:
              minimum: "is too short"
